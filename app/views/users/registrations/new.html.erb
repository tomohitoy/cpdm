<div class="row">
<div class="large-8 columns">
	<div class="panel">
		<h2>登録する</h2>
		<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  		<%= devise_error_messages! %>
		<%= f.email_field :email, :placeholder=> 'メールアドレス', :autofocus => true %>
		<%= f.password_field :password, :placeholder=>'パスワード' %>
		<%= f.password_field :password_confirmation, :placeholder=>'パスワード(確認)' %>
		<div><%= f.submit "登録", :class=>'button' %></div>
		<% end %>
	</div>
</div>
<div class="large-4 columns">
	<div class="panel">
		<p>
		左記に情報を入力し、登録を完了させてください。
		</p>
		<p>アカウントをお持ちの方は<br/>
		<%= link_to "トップページへ", root_path %>
		</p>
		<p>パスワードをお忘れですか？<br/>
		<%= link_to "パスワードの再発行", new_password_path(resource_name) %>
		</p>
	</div>
</div>
</div>
